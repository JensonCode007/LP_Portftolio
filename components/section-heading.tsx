export function SectionHeading({ index, eyebrow, title, className = "" }: { index: string; eyebrow: string; title: string; className?: string }) { return <div className={`mb-10 flex items-end justify-between gap-6 border-b border-white/15 pb-4 ${className}`}><div><p className="mono mb-3 text-[10px] uppercase tracking-[.16em] text-white/45">{index} / {eyebrow}</p><h2 className="display text-4xl md:text-6xl">{title}</h2></div><span className="mono hidden text-[10px] uppercase text-white/40 sm:block">Scroll to explore</span></div>; }
